# Arquivo de migrations anterior à baseline

Estes arquivos são preservados apenas para auditoria e para rastrear decisões
históricas. O Supabase CLI executa exclusivamente `supabase/migrations/`.

A migration-base original que precedia `20250531112404` não está no Git, por
isso esta cadeia não consegue iniciar um banco vazio. A baseline de 2026-08-27
é a nova fonte executável e foi validada contra o schema remoto sem dados.
